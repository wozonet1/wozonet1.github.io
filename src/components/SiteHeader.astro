---
const path = Astro.url.pathname;
const links = [
  { href: "/", label: "首页" },
  { href: "/daily/", label: "日常" },
  { href: "/posts/", label: "文章" },
  { href: "/tags/", label: "主题" },
  { href: "/about/", label: "关于" },
];
---

<header class="site-header">
  <div class="shell nav-shell">
    <a class="brand" href="/" aria-label="Wozonet 首页">
      <span class="brand-mark" aria-hidden="true"></span>
      <span>wozonet</span>
    </a>
    <nav aria-label="主导航">
      {
        links.map((link) => (
          <a
            href={link.href}
            aria-current={
              path === link.href || (link.href !== "/" && path.startsWith(link.href))
                ? "page"
                : undefined
            }
          >
            {link.label}
          </a>
        ))
      }
    </nav>
    <button class="theme-toggle" type="button" data-theme-toggle aria-label="切换到浅色模式">
      <span aria-hidden="true"></span>
    </button>
  </div>
</header>

<style>
  .site-header {
    --header-page-bg: var(--bg);
    --header-text: oklch(0.9596 0.0131 82.4);
    --header-muted: oklch(0.9596 0.0131 82.4 / 0.66);
    --header-line: oklch(0.9596 0.0131 82.4 / 0.14);
    --header-accent: var(--accent);
    --header-shadow: oklch(0 0 0 / 0.24);
    position: sticky;
    z-index: 30;
    top: 0;
    border-bottom: 1px solid var(--header-line);
    background: color-mix(in oklch, var(--header-page-bg) 86%, transparent);
    box-shadow:
      0 1px 0 var(--header-line),
      0 0.7rem 1.5rem -0.65rem var(--header-shadow);
    color: var(--header-text);
    backdrop-filter: blur(18px);
    transition:
      color 180ms ease,
      border-color 180ms ease,
      background-color 180ms ease,
      box-shadow 180ms ease;
  }

  .site-header[data-contrast="dark"] {
    --header-text: oklch(0.2787 0.0219 57.72);
    --header-muted: oklch(0.2787 0.0219 57.72 / 0.66);
    --header-line: oklch(0.2787 0.0219 57.72 / 0.14);
    --header-accent: oklch(0.5417 0.1578 37.05);
    --header-shadow: oklch(0.2787 0.0219 57.72 / 0.34);
  }

  .nav-shell {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    align-items: center;
    min-height: 4.25rem;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    justify-self: start;
    gap: 0.6rem;
    font-family: var(--mono);
    font-size: 0.84rem;
    font-weight: 700;
    letter-spacing: 0.03em;
    text-decoration: none;
  }

  .brand-mark {
    width: 0.72rem;
    height: 0.72rem;
    border: 1px solid var(--header-accent);
    border-radius: 50%;
    box-shadow:
      inset 0 0 0 0.2rem var(--header-page-bg),
      inset 0 0 0 0.5rem var(--header-accent);
  }

  nav {
    display: flex;
    gap: 1.5rem;
  }

  nav a {
    position: relative;
    padding-block: 1.35rem;
    color: var(--header-muted);
    font-size: 0.78rem;
    text-decoration: none;
  }

  nav a::after {
    position: absolute;
    right: 0;
    bottom: -1px;
    left: 0;
    height: 1px;
    transform: scaleX(0);
    background: var(--header-accent);
    content: "";
    transition: transform 160ms ease;
  }

  nav a:hover,
  nav a[aria-current="page"] {
    color: var(--header-text);
  }

  nav a:hover::after,
  nav a[aria-current="page"]::after {
    transform: scaleX(1);
  }

  .theme-toggle {
    display: grid;
    width: 2.25rem;
    height: 2.25rem;
    place-items: center;
    justify-self: end;
    border: 1px solid var(--header-line);
    border-radius: 50%;
    background: transparent;
    color: var(--header-text);
    cursor: pointer;
  }

  .theme-toggle span {
    width: 0.8rem;
    height: 0.8rem;
    border: 1px solid currentColor;
    border-radius: 50%;
    background: linear-gradient(90deg, currentColor 50%, transparent 50%);
  }

  @media (max-width: 42rem) {
    .nav-shell {
      grid-template-columns: 1fr auto;
    }

    nav {
      position: fixed;
      right: 0;
      bottom: 0;
      left: 0;
      z-index: 40;
      justify-content: space-around;
      gap: 0;
      border-top: 1px solid var(--header-line);
      background: color-mix(in oklch, var(--header-page-bg) 92%, transparent);
      backdrop-filter: blur(18px);
    }

    nav a {
      flex: 1;
      padding-block: 0.85rem;
      text-align: center;
    }

    nav a::after {
      top: -1px;
      bottom: auto;
    }

  }
</style>
